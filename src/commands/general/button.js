module.exports ={
name :"button",
code : $djseval[const { MessageButton } = require('discord-buttons'); //add 2 "" around this code 


 let button = new disbut.MessageButton()
 .setStyle('red') //default: blurple
 .setLabel('Boton') //default: NO_LABEL_PROVIDED
 .setID('click_to_function') //note: if you use the style "url" you must provide url using .setURL('https://example.com')


 await message.channel.send('esto es una prueba);
 }
}]